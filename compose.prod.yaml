services:
  database:
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.8'
          memory: 350M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 10
        window: 5s

  webserver:
    restart: always
    volumes:
      # Shared files between deployments
      - source_uploads:/app/public/uploads # For example, files uploaded by users
      # Shared cache between deployments
      - share_dir_cache:/app/${APP_SHARE_DIR}
    depends_on:
      - database
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 250M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 10
        window: 5s

  worker_async:
    image: ${DOCKER_IMAGE_NAME_WEBSERVER}
    entrypoint: '/usr/local/bin/worker_async_entrypoint.sh'
    restart: always
    healthcheck:
      test: [ "CMD", "php", "bin/console", "about" ]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      - database
    deploy:
      resources:
        limits:
          cpus: '0.7'
          memory: 175M
    volumes:
      # Shared cache between deployments
      - share_dir_cache:/app/${APP_SHARE_DIR}

  worker_scheduler:
    image: ${DOCKER_IMAGE_NAME_WEBSERVER}
    entrypoint: '/usr/local/bin/worker_scheduler_entrypoint.sh'
    restart: always
    healthcheck:
      test: [ "CMD", "php", "bin/console", "about" ]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      # Shared cache between deployments
      - share_dir_cache:/app/${APP_SHARE_DIR}
    depends_on:
      - database
    deploy:
      resources:
        limits:
          cpus: '0.7'
          memory: 175M

volumes:
  source_uploads:
  share_dir_cache:
