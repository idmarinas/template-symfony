framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - assets/
            - apps/web/assets/
        missing_import_mode: strict
        precompress:
            format: gzip

when@prod:
    framework:
        asset_mapper:
            missing_import_mode: warn
