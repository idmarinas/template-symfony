symfonycasts_tailwind:
    # Specify the EXACT version of Tailwind CSS you want to use
    binary_version: 'v4.1.5'
    input_css:
        # Default:
        - '%kernel.project_dir%/assets/styles/app.css'
        # Others
        - '%kernel.project_dir%/assets/entries/error_exception/error_exception.css'

    # Alternatively, you can specify the path to the binary that you manage yourself
    #binary: 'node_modules/.bin/tailwindcss'
